byte packet[94] = {
  0x00, 0x01, 0x00, 0x2a, 0x00, 0x0d, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x20, 0x11, 0x1e, 0x11, 0x23, 0x1f,
  0x1e, 0x19, 0x13, 0x00, 0x00, 0x00, 0x01, 0x00, 0x00, 0x00,
  0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0xff, 0xf0,
  0x00, 0x26, 0x00, 0x20, 0x00, 0x21, 0x00, 0x22, 0x00, 0x23,
  0x00, 0x25, 0x00, 0x28, 0x00, 0x40, 0x00, 0x41, 0x00, 0x42,
  0x00, 0x44, 0x00, 0xa5, 0x00, 0xa6, 0x00, 0xa7, 0x00, 0xa8,
  0x00, 0xad, 0x00, 0xb3, 0x00, 0xb4, 0x00, 0xb7, 0x00, 0xb8,
  0xff, 0xff, 0x11, 0xe2
};
byte mac[] = {
  0xfc, 0xaa, 0x14, 0x78, 0x05, 0xae
};
uint8_t ip[] = {
  0xc0, 0xa8, 0x01, 0x64
};

void setup() {
  delay(10000);
  packet[12] = mac[0];
  packet[13] = mac[1];
  packet[14] = mac[2];
  packet[15] = mac[3];
  packet[16] = mac[4];
  packet[17] = mac[5];

  packet[18] = ip[0];
  packet[19] = ip[1];
  packet[20] = ip[2];
  packet[21] = ip[3];

  calculate_checksum();
}

void loop() {

}

void calculate_checksum() {
  unsigned int SUM = 0;
  byte SSUM[2];
  int i;
  for (i = 0; i < 92; i = i + 1) {
    Serial.print("Packet.");
    Serial.print(i);
    Serial.print("  :   ");
    Serial.print(packet[i], HEX);
    Serial.print("   ->  ");
    Serial.println(packet[i], DEC);
    SUM = SUM + int(packet[i]);
  }
  Serial.println(SUM, DEC);
  Serial.print("Checksum:");
  Serial.print(SUM + 1, DEC);
  Serial.print(":HEX  ");
  Serial.println(SUM + 1, HEX);

  packet[92] = byte( SUM+1 >> 8 );
  packet[93] = byte( SUM+1 );
  Serial.print("Packet Checksum  ");
  Serial.print(packet[92], HEX);
  Serial.println(packet[93], HEX);

}

